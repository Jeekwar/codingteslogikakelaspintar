const MyFunc()=>{
let n =[];
for(let i=0; i<=n; i++) {
    let target = n;
    while (target < n && target != array[target]){
        new_target = array[target];
        array[target] = target;
        target = new_target;
    }
        
}
for (i-0; i<=n){
        if (array[i] != i){
            return i}
    return n;
}
}
